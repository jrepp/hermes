---
id: rfc-NNN
title: "<Descriptive title — what is being proposed or specified>"
status: Draft
created: YYYY-MM-DD
author: Hermes Team
project_id: hermes
doc_uuid: <run `uuidgen | tr '[:upper:]' '[:lower:]'`>
type: RFC
subtype: Architecture
tags: [tag-one, tag-two]
related:
  - ADR-NNN
  - RFC-NNN
---

# RFC-NNN: <Same title as frontmatter>

> One paragraph (2–4 lines) summarizing what this RFC proposes or specifies, and what reader should care. If this RFC carries the narrative for an existing ADR, say so: *"The binding architectural rule lives in [ADR-NNN](../adr/adr-NNN-<slug>.md); this RFC preserves the narrative, benchmarks, and migration plan."*

## Motivation

Why this proposal exists. What problem it solves, what's broken or missing today, what we'd lose by not doing it. Cite real symptoms — code paths, incident memos, user complaints.

## Goals

- Concrete, observable outcome one.
- Concrete, observable outcome two.
- Concrete, observable outcome three.

## Non-Goals

- Out-of-scope item one.
- Out-of-scope item two.

## Proposal

The body of the RFC. Use sub-headings freely. Typical sections:

### Overview

A 1–2 paragraph plain-English description of the design.

### Detailed Design

Types, interfaces, data flow, boundary contracts. Diagrams (ASCII or mermaid) are welcome here. Code examples are welcome here.

```text
┌──────────┐     ┌──────────┐
│ Client   │ ──► │ Backend  │
└──────────┘     └──────────┘
```

```go
// types, interfaces, examples
```

### API / Schema Changes

If the proposal touches the API surface or DB schema, document the diffs explicitly: new endpoints, deprecated endpoints, new columns, indexes, migrations.

### Migration Plan

Phases, ordering, backward-compatibility window, kill-switch / rollback strategy. Real timelines are welcome.

### Performance / Scalability (if applicable)

Measured numbers, benchmark methodology, projected load. Don't make up numbers — link to the benchmark file or omit.

### Security / Privacy Considerations (if applicable)

Threat model deltas, secrets handling, PII surface area.

## Alternatives Considered

- **<Alternative A>** — explored; rejected because <reason>.
- **<Alternative B>** — explored; rejected because <reason>.
- **<Alternative C>** — explored; rejected because <reason>.

## Open Questions

- Question one — who owns answering it, by when.
- Question two.

(Once answered, move into the body and delete the question.)

## References

- [ADR-NNN: <title>](../adr/adr-NNN-<slug>.md) — binding rule for this work.
- [RFC-NNN: <title>](rfc-NNN-<slug>.md) — related proposal.
- [MEMO-NNN: <title>](../memo/memo-NNN-<slug>.md) — supporting investigation.
- Code: `pkg/foo/`, `internal/bar/`.

<!--
AUTHORING REMINDERS — delete before committing.

DO:
- Treat the RFC as the long form. ADRs are tight; RFCs carry the narrative.
- Use diagrams, code samples, and tables freely — this is the right place for them.
- Link out to memos for incident reports and one-off investigations.
- Update `rfc-002-readme.md` in the same change.
- When this RFC is implemented, update `status: Implemented` and ensure a corresponding ADR captures the binding rule.

DO NOT:
- Restate the binding rule from the ADR verbatim — link to it.
- Include parenthetical status suffixes like `Implemented (Phase 1)` — use `Implemented` and explain phases in the body.
- Include AI co-author footers or "generated by …" markers.
-->
