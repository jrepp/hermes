/hermes

# Build artifacts directory
# Exclude all contents but keep structure files
/build/*
!/build/README.md
!/build/.gitkeep
!/build/bin/
!/build/coverage/
!/build/reports/
!/build/test/
!/build/logs/
!/build/tmp/
/build/bin/*
/build/coverage/*
/build/reports/*
/build/test/*
/build/logs/*
/build/tmp/*
!/build/**/.gitkeep
!/build/**/README.md

# Local configuration area (RFC: multi-domain hosting).
# Everything under local/ is developer- or deployment-specific and may hold
# secrets. Only the committed examples and readme are tracked.
/local/*
!/local/readme.md
!/local/config.example.hcl
!/local/sites
/local/sites/*
!/local/sites/*.example.hcl

# Root config.hcl is a local artifact created from config-example.hcl.
/config.hcl

# Google OAuth 2.0
/credentials.json
/token.json

# Setup scratch dir (embedded Bleve indexes, bbolt stores)
/.hermes-setup-tmp/

# macOS & local
.DS_Store

# Environment variables (secrets)
.env
.env.local
.env.*.local
.env.development
.env.staging
.env.production
.env.test

# TLS certs (local dev)
/certs

# Web application
node_modules
/web/.pnp.*
/web/.yarn/*
/web/.eslintcache
# Frontend build output. `.gitkeep` is committed so the `//go:embed all:dist`
# in web/web.go resolves on a clean checkout (see `make web/stub`).
/web/dist/*
!/web/dist/.gitkeep

# Test artifacts
*.test
*.out
coverage*.html
coverage*.out
test-output.log

# Playwright test artifacts
/tests/e2e-playwright/test-results/
/tests/e2e-playwright/playwright-report/
/tests/e2e-playwright/.playwright/
/tests/e2e-playwright/playwright/.cache/
**/test-results/
**/.playwright/
.playwright-mcp/

# Terraform related
.terraform

# Backup files
*.bak
*.bak[0-9]
*.bak[0-9][0-9]
*.orig
*.rej
*backup

# Test logs
test_*.log

# Patch files
*.patch

/web/dist-test

# Playwright MCP screenshots and artifacts
.playwright-mcp/

# Volume data
/workspace_data/

# Project workspaces (testing environment)
/testing/workspaces/*
!/testing/workspaces/readme.md

# Old testing build artifacts (deprecated, moved to /build)
/testing/build-artifacts/

# Distributed projects configuration (internal/production)
# HCL-based modular configs with safe examples in testing/projects/
# Protect production and internal configs from accidental commits

# Production/internal project configs
/projects.hcl
/projects.local.hcl
/projects.production.hcl
/projects.internal.hcl
/testing/projects.local.hcl
/testing/projects.production.hcl
/testing/projects.internal.hcl
**/projects.production.hcl
**/projects.internal.hcl
**/projects.local.hcl

# Individual project files with real credentials
/projects/**/*.local.hcl
/projects/**/*.production.hcl
/projects/**/*.internal.hcl
/testing/projects/*.local.hcl
/testing/projects/*.production.hcl
/testing/projects/*.internal.hcl

# Old JSON format (deprecated, use HCL instead)
/projects.json
/projects.local.json
/projects.*.json
/projects.production.json
/projects.internal.json
/testing/projects.local.json
/testing/projects.production.json
/testing/projects.internal.json
**/projects.production.json
**/projects.internal.json
**/projects.local.json

/notifier
